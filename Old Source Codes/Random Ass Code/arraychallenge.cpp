#include <bits/stdc++.h>

int main() {
    /* int N;
    int Max_Value = INT8_MIN;
    std::cin >> N;
    int A[N];
        for (int i = 0; i < N; i++) {
            std::cin >> A[i];
        }
        for (int i = 0; i < N; i++) {
            Max_Value = std::max(Max_Value, A[i]);
        }
        std::cout << Max_Value; */
        long long max = INT64_MAX;
        std::cout << max;
}