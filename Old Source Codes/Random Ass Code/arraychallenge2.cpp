/* Problem: Given an unsorted array A of size N of non-negative integers, find a continuous sub_array
which adds to a given number S.*/

#include <iostream>

int main() {
    int N; int T;
    std::cin >> T;
        while (T--) {
            std::cin >> N;
            int A[N];
            
            for (int i = 0; i < N; i++) {
                std::cin >> A[i];
            }
    }

    return 0;
}