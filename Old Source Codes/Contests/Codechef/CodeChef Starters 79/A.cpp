#include <bits/stdc++.h>

using namespace std;

class Solution {
    public:
        void test_cases() {
            int N;
            cout << abs(N - 10) << endl;
        }
};

signed main(void) {
    if (true) {
        ios_base::sync_with_stdio(false);
        cin.tie(NULL); cout.tie(0);
    }

    int tc;
    cin >> tc;
    Solution run;
        while (tc--) {
            run.test_cases();
        }
    return 0;
}