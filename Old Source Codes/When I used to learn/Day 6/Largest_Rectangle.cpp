// Problem: Largest Rectangle in a histogram
// Given an array. Each element represents
// the height to the histogram's bar and the
// width of each bar is 1, find the area of
// largest rectangle in the histogram.

#include <bits/stdc++.h>

using namespace std;

class Solution {
    public:
        int largestRectangleArea(vector<int>& heights) {
            
        }
};

signed main(void) {
    Solution s;
    vector<int> heights = {2,1,5,6,2,3};
    cout << s.largestRectangleArea(heights);
}

// Input: heights = [2,1,5,6,2,3]
// Output: 10