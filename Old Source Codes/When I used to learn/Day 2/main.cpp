#include "L1_Permutations.cpp"
#include "L2_Vectors_Pairs.cpp"

int main(void) {

    vector<int> V = {1, 2, 2};
    // permutations(V, 0);
    // STLpermutations(V);
    // permutationsDUPLICATE(V, 0);
    // pairs();
    // vectors();
    manyVec();
}